---
phase: 01-terminal-runtime-and-panes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Wcmux.sln
  - src/Wcmux.App/Wcmux.App.csproj
  - src/Wcmux.App/App.xaml
  - src/Wcmux.App/App.xaml.cs
  - src/Wcmux.App/MainWindow.xaml
  - src/Wcmux.App/MainWindow.xaml.cs
  - src/Wcmux.Core/Wcmux.Core.csproj
  - src/Wcmux.Core/Runtime/SessionLaunchSpec.cs
  - src/Wcmux.Core/Runtime/SessionEvent.cs
  - src/Wcmux.Core/Runtime/ConPtyHost.cs
  - src/Wcmux.Core/Runtime/SessionManager.cs
  - tests/Wcmux.Tests/Wcmux.Tests.csproj
  - tests/Wcmux.Tests/Runtime/SessionHostIntegrationTests.cs
  - tests/Wcmux.Tests/Runtime/SessionLifecycleTests.cs
autonomous: true
requirements:
  - SESS-01
user_setup: []
must_haves:
  truths:
    - App startup opens one ready-to-use default PowerShell pane through a ConPTY-backed session.
    - The runtime emits typed ready, output, cwd-changed, resized, and exited events without UI polling.
    - Repeated session open and close cycles clean up pseudoconsole and child-process resources without orphaned shells.
  artifacts:
    - path: Wcmux.sln
      provides: Solution wiring for the app, runtime core, and test projects
      contains: Project(
    - path: src/Wcmux.Core/Runtime/ConPtyHost.cs
      provides: ConPTY creation, resize, shutdown, and handle cleanup
      min_lines: 80
      contains: CreatePseudoConsole
    - path: src/Wcmux.Core/Runtime/SessionManager.cs
      provides: Session lifecycle orchestration and typed event fan-out
      min_lines: 80
      contains: CreateSessionAsync
    - path: src/Wcmux.App/MainWindow.xaml.cs
      provides: Native shell startup that creates the first terminal session
      min_lines: 40
      contains: CreateInitialSessionAsync
    - path: tests/Wcmux.Tests/Runtime/SessionHostIntegrationTests.cs
      provides: Real-session integration coverage for launch, IO, exit, and cleanup
      min_lines: 60
      contains: SessionHost
  key_links:
    - from: src/Wcmux.App/MainWindow.xaml.cs
      to: src/Wcmux.Core/Runtime/SessionManager.cs
      via: startup bootstraps the initial session before the shell is interactive
      pattern: CreateInitialSessionAsync
    - from: src/Wcmux.Core/Runtime/SessionManager.cs
      to: src/Wcmux.Core/Runtime/ConPtyHost.cs
      via: session creation and teardown delegate to the ConPTY host boundary
      pattern: ConPtyHost
    - from: tests/Wcmux.Tests/Runtime/SessionHostIntegrationTests.cs
      to: src/Wcmux.Core/Runtime/SessionManager.cs
      via: integration harness exercises the real session lifecycle
      pattern: SessionManager
---

<objective>
Establish the native Windows app bootstrap and shell-agnostic ConPTY runtime that Phase 1 depends on.

Purpose: Make terminal fidelity a runtime concern first so later pane and renderer work builds on a real session host instead of a fake terminal wrapper.
Output: A WinUI 3 solution skeleton, typed session lifecycle contracts, ConPTY-backed session management, default PowerShell startup, cwd tracking hooks, and runtime tests.
</objective>

<execution_context>
@C:/Users/Jack/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Jack/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-terminal-runtime-and-panes/01-CONTEXT.md
@.planning/phases/01-terminal-runtime-and-panes/01-RESEARCH.md
@.planning/phases/01-terminal-runtime-and-panes/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Windows solution, runtime core, and test harness</name>
  <files>Wcmux.sln, src/Wcmux.App/Wcmux.App.csproj, src/Wcmux.App/App.xaml, src/Wcmux.App/App.xaml.cs, src/Wcmux.App/MainWindow.xaml, src/Wcmux.App/MainWindow.xaml.cs, src/Wcmux.Core/Wcmux.Core.csproj, tests/Wcmux.Tests/Wcmux.Tests.csproj</files>
  <action>Create the initial WinUI 3 app, a separate runtime/core library, and the xUnit test project referenced in `01-VALIDATION.md`. Keep the app shell minimal but real: the main window should expose one root terminal host slot and request an initial session on launch instead of waiting for a later manual open flow. Wire project references, solution membership, and any shared abstractions so later plans can add renderer and layout code without reshaping the solution.</action>
  <verify>dotnet build Wcmux.sln</verify>
  <done>The repository has a buildable solution with app, core, and test projects, and the app startup path has a clear hook for creating the first session.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the ConPTY-backed session lifecycle and default shell bootstrap</name>
  <files>src/Wcmux.Core/Runtime/SessionLaunchSpec.cs, src/Wcmux.Core/Runtime/SessionEvent.cs, src/Wcmux.Core/Runtime/ConPtyHost.cs, src/Wcmux.Core/Runtime/SessionManager.cs, src/Wcmux.App/MainWindow.xaml.cs</files>
  <action>Implement a shell-agnostic runtime boundary around `SessionLaunchSpec`, `SessionManager`, and `ConPtyHost` using typed events for ready, output, cwd changes, resize, and exit. Launch the default PowerShell profile through ConPTY, start input and output pumps before the UI begins interacting with the session, and include the PowerShell-first cwd reporting contract described in research so later splits can inherit the last known working directory instead of guessing from process state. Include graceful shutdown plus a forced cleanup fallback for hung shells.</action>
  <verify>dotnet test tests/Wcmux.Tests/Wcmux.Tests.csproj --filter FullyQualifiedName~SessionHost</verify>
  <done>The app can request a real PowerShell session through ConPTY, the runtime surfaces typed lifecycle events, and shutdown paths dispose handles deterministically.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime integration coverage for launch, IO, exit, and cleanup loops</name>
  <files>tests/Wcmux.Tests/Runtime/SessionHostIntegrationTests.cs, tests/Wcmux.Tests/Runtime/SessionLifecycleTests.cs</files>
  <action>Create the Phase 1 Wave 0 runtime tests called out in `01-VALIDATION.md`. Cover shell launch, input/output round-tripping, exit observation, cwd signal capture, and repeated open-close loops so SessionManager and ConPtyHost do not regress under the repeated-session smoke matrix described in research. Keep the tests real-process based where possible and isolate timing-sensitive assertions to avoid flaky behavior.</action>
  <verify>dotnet test tests/Wcmux.Tests/Wcmux.Tests.csproj --filter FullyQualifiedName~SessionLifecycle</verify>
  <done>The runtime harness proves the ConPTY host launches PowerShell, accepts IO, emits lifecycle events, and survives repeated teardown without orphaned resources.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build Wcmux.sln
- [ ] dotnet test tests/Wcmux.Tests/Wcmux.Tests.csproj --filter FullyQualifiedName~SessionHost
- [ ] dotnet test tests/Wcmux.Tests/Wcmux.Tests.csproj --filter FullyQualifiedName~SessionLifecycle
- [ ] The app startup path requests one default shell session automatically instead of showing an empty shell surface.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `SESS-01` is covered by a real ConPTY-backed startup path rather than plain process pipes
- The runtime has explicit seams for later renderer attachment and pane-level session orchestration
- The project has the test bootstrap required by `01-VALIDATION.md` for later plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-terminal-runtime-and-panes/01-01-SUMMARY.md`
</output>
