---
phase: 01-terminal-runtime-and-panes
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/Wcmux.App/MainWindow.xaml
  - src/Wcmux.App/MainWindow.xaml.cs
  - src/Wcmux.App/Views/TerminalPaneView.xaml
  - src/Wcmux.App/Views/TerminalPaneView.xaml.cs
  - src/Wcmux.App/Terminal/TerminalSurfaceBridge.cs
  - src/Wcmux.App/Terminal/WebViewTerminalController.cs
  - src/Wcmux.App/TerminalWeb/index.html
  - src/Wcmux.App/TerminalWeb/terminal-host.js
  - tests/Wcmux.Tests/Terminal/TerminalBridgeTests.cs
  - tests/Wcmux.Tests/Terminal/ResizePipelineTests.cs
  - tools/Run-Phase01TerminalSmoke.ps1
autonomous: true
requirements:
  - SESS-02
user_setup: []
must_haves:
  truths:
    - Terminal panes render ConPTY VT output through WebView2 and remain interactive for typing, paste, and process exit.
    - Cell-size negotiation and debounced resize updates keep terminal rows and columns synchronized with pane bounds.
    - The phase includes repeatable automated and scripted smoke checks for alternate-screen TUI behavior and aggressive resize scenarios.
  artifacts:
    - path: src/Wcmux.App/Views/TerminalPaneView.xaml.cs
      provides: Native pane host that attaches a WebView2 terminal surface to a live session
      min_lines: 70
      contains: WebView2
    - path: src/Wcmux.App/Terminal/TerminalSurfaceBridge.cs
      provides: Bidirectional bridge for VT output, terminal input, cwd signals, and resize updates
      min_lines: 80
      contains: cwdChanged
    - path: src/Wcmux.App/TerminalWeb/index.html
      provides: Hosted xterm surface entrypoint used by each pane
      min_lines: 20
      contains: xterm
    - path: tests/Wcmux.Tests/Terminal/TerminalBridgeTests.cs
      provides: Automated coverage for output forwarding, input routing, and cwd signal parsing
      min_lines: 60
      contains: TerminalBridge
    - path: tests/Wcmux.Tests/Terminal/ResizePipelineTests.cs
      provides: Automated coverage for resize negotiation and redundant resize suppression
      min_lines: 60
      contains: ResizePipeline
    - path: tools/Run-Phase01TerminalSmoke.ps1
      provides: Scripted smoke harness for vim, fzf, paste, and resize checks called out in validation
      min_lines: 30
      contains: vim
  key_links:
    - from: src/Wcmux.App/Views/TerminalPaneView.xaml.cs
      to: src/Wcmux.App/Terminal/WebViewTerminalController.cs
      via: pane activation creates and attaches the hosted terminal surface
      pattern: AttachAsync
    - from: src/Wcmux.App/Terminal/TerminalSurfaceBridge.cs
      to: src/Wcmux.Core/Runtime/SessionManager.cs
      via: runtime output, input, cwd, and resize events flow through the bridge instead of the UI polling
      pattern: SessionManager
    - from: tests/Wcmux.Tests/Terminal/ResizePipelineTests.cs
      to: src/Wcmux.App/Terminal/TerminalSurfaceBridge.cs
      via: automated tests prove resize debounce and row-column translation behavior
      pattern: Resize
---

<objective>
Add the hosted terminal surface, IO bridge, and resize pipeline needed for full-screen terminal fidelity.

Purpose: Turn the ConPTY runtime from Plan 01 into a real terminal experience that can survive alternate-screen TUIs, paste, and aggressive resize behavior.
Output: A WebView2-hosted xterm surface per pane, a bridge between runtime and renderer, resize negotiation, bridge tests, and the scripted smoke harness from the phase validation strategy.
</objective>

<execution_context>
@C:/Users/Jack/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Jack/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-terminal-runtime-and-panes/01-CONTEXT.md
@.planning/phases/01-terminal-runtime-and-panes/01-RESEARCH.md
@.planning/phases/01-terminal-runtime-and-panes/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Host a WebView2-backed xterm surface for each terminal pane</name>
  <files>src/Wcmux.App/MainWindow.xaml, src/Wcmux.App/MainWindow.xaml.cs, src/Wcmux.App/Views/TerminalPaneView.xaml, src/Wcmux.App/Views/TerminalPaneView.xaml.cs, src/Wcmux.App/Terminal/WebViewTerminalController.cs, src/Wcmux.App/TerminalWeb/index.html, src/Wcmux.App/TerminalWeb/terminal-host.js</files>
  <action>Implement the native pane host described in research using WebView2 plus xterm.js behind a small controller boundary. Keep xterm details out of the runtime core, give each pane its own attachable surface, and make the host own focus transitions and lifecycle instead of burying those concerns in DOM glue. The first working version should attach the root session from Plan 01 and render live terminal output inside the window.</action>
  <verify>dotnet build Wcmux.sln</verify>
  <done>The app can attach a live session to a pane-local terminal surface and show real terminal output in the hosted renderer.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the terminal bridge for output batching, input routing, and resize negotiation</name>
  <files>src/Wcmux.App/Terminal/TerminalSurfaceBridge.cs, src/Wcmux.App/Views/TerminalPaneView.xaml.cs, src/Wcmux.App/Terminal/WebViewTerminalController.cs</files>
  <action>Bridge ConPTY output, user input, paste, cwd signals, and pane-size updates between the runtime and hosted terminal surface. Batch output writes enough to avoid UI-thread churn, keep shortcut interception above the terminal surface, and implement the resize pipeline from research: measure cell size, translate pane bounds to rows and columns, debounce resize spam, and skip redundant `ResizePseudoConsole` calls. Preserve alternate-screen and exit behavior so the bridge remains transparent to tools like `vim` and `fzf`.</action>
  <verify>dotnet test tests/Wcmux.Tests/Wcmux.Tests.csproj --filter FullyQualifiedName~TerminalBridge</verify>
  <done>Terminal IO flows both directions through the bridge, cwd updates remain observable, and resize events are translated into stable row-column updates instead of raw pixel spam.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated bridge coverage and the scripted Phase 1 smoke harness</name>
  <files>tests/Wcmux.Tests/Terminal/TerminalBridgeTests.cs, tests/Wcmux.Tests/Terminal/ResizePipelineTests.cs, tools/Run-Phase01TerminalSmoke.ps1</files>
  <action>Add the automated test filters named in `01-VALIDATION.md` for terminal bridging and resize behavior, then create the scripted smoke harness that exercises the manual-only matrix from research and validation: launch `vim`, launch `fzf` or an equivalent alternate-screen selector, paste input, and aggressively resize while sessions remain live. The script can drive repeatable setup and commands even if a human still observes the UI outcome later.</action>
  <verify>dotnet test tests/Wcmux.Tests/Wcmux.Tests.csproj --filter FullyQualifiedName~ResizePipeline</verify>
  <done>The codebase has automated bridge and resize tests plus a reusable smoke harness for the Phase 1 TUI fidelity matrix.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build Wcmux.sln
- [ ] dotnet test tests/Wcmux.Tests/Wcmux.Tests.csproj --filter FullyQualifiedName~TerminalBridge
- [ ] dotnet test tests/Wcmux.Tests/Wcmux.Tests.csproj --filter FullyQualifiedName~ResizePipeline
- [ ] dotnet test
- [ ] `tools/Run-Phase01TerminalSmoke.ps1` exists and covers `vim`, `fzf` or an equivalent selector, paste, and aggressive resize scenarios.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `SESS-02` is supported by a real terminal renderer and bridge, not a text-box approximation
- The runtime and renderer remain separated by a narrow surface adapter seam
- The validation strategy's automated and scripted smoke coverage exists before pane-layout work builds on the terminal surface
</success_criteria>

<output>
After completion, create `.planning/phases/01-terminal-runtime-and-panes/01-02-SUMMARY.md`
</output>
